# M4 配额 429 Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** 月度 `monthly_tokens` 超限时 chat/search 返回 429 `quota_exceeded`。

**Architecture:** `QuotaService` 读 hourlies(+fallback) + Redis 300s；`require_token_quota` Depends；`QuotaExceeded` + Retry-After。

**Tech Stack:** FastAPI、SQLAlchemy、Redis、现有 usage 表。

## File map

| 文件 | 职责 |
| --- | --- |
| `platform/errors.py` | `QuotaExceeded` + handler Retry-After |
| `modelops/quota.py` | 检查逻辑与缓存 |
| `platform/deps.py` | `require_token_quota` / `TokenQuotaDep` |
| `chat/router.py` / `retrieval/router.py` | 挂载 |
| `tests/test_quota.py` | 集成测试 |
| `docs/07-progress.md` | 进度 |

### Task 1: QuotaExceeded + QuotaService + deps + 路由
### Task 2: 测试 + 进度文档
