# Ops UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 `/admin` 三 Tab 运营页（接入点 / 成员 / 审计）。

**Architecture:** Admin 壳读 `?tab=`；modelops 与 admin feature 分域；TanStack Query + OpenAPI 类型。

**Tech Stack:** React 18、TanStack Query、现有 Tailwind、FastAPI OpenAPI。

## Global Constraints

- 仅 owner/admin；凭证只写不回显；平台接入点只读
- test 看 body.ok；priority 越小越优先
- 不含 KB grants / 邀请注册

---

### Task 1: 基础设施

- [ ] `api.put`；修 worker F401
- [ ] modelops + memberships + audit api/hooks

### Task 2: UI

- [ ] ConnectionsPanel / MembersPanel / AuditPanel
- [ ] AdminPage + 路由导航 redirect
- [ ] typecheck / lint / 更新 07-progress

---
